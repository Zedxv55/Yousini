# YOUSINI.example.md

ไฟล์นี้คือ **บริบทโปรเจกต์ถาวร** (เหมือน `CLAUDE.md` ของ Claude Code)
คัดลอกเป็น `YOUSINI.md` แล้วใส่กฎ/ข้อตกลงของโปรเจกต์คุณ — Yousini จะโหลดไฟล์นี้
อัตโนมัติทุกครั้งที่เริ่มงานในโฟลเดอร์นี้ (และโฟลเดอร์แม่จนถึง root) รวมถึง `~/.yousini.md` ระดับเครื่อง

ตัวอย่างสิ่งที่ใส่ได้:

- **ภาษา:** ตอบและเขียนโค้ดเป็นภาษาไทย
- **Stack:** โปรเจกต์นี้ใช้ FastAPI + SQLAlchemy + PostgreSQL
- **กฎการเขียนโค้ด:** ห้ามใช้ `print` สำหรับ log ให้ใช้ `logging` เสมอ
- **การทดสอบ:** รัน `pytest` ก่อน commit เสมอ
- **Style:** ใช้ snake_case สำหรับฟังก์ชัน, PascalCase สำหรับคลาส
- **ห้าม:** อย่าเพิ่ม dependency ใหม่โดยไม่แจ้ง

เมื่อใดก็ตามที่โมเดลเริ่มสับสนกับ convention ของโปรเจกต์ ให้อ้างอิงไฟล์นี้
